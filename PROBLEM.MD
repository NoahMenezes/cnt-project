2. Secure Messaging Application

Problem Statement:
Normal messaging systems can expose user messages if servers are compromised. Build a secure messaging application using hybrid encryption where messages are encrypted before transmission and decrypted only by intended users.

Features:

Real-time chat
RSA key generation
AES message encryption
User login/authentication
Chat history storage